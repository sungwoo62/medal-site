---
phase: 02-hero-image
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/hero-bg.jpg
  - src/app/page.tsx
autonomous: false
requirements:
  - HERO-01
  - HERO-02
must_haves:
  truths:
    - "Hero section displays a medal-related background image on desktop"
    - "Hero section background image crops/resizes appropriately on mobile"
    - "White hero text remains readable over the image with dark overlay"
    - "Hero section renders gracefully if image fails to load (dark gradient fallback)"
  artifacts:
    - path: "public/hero-bg.jpg"
      provides: "Hero background image asset"
      contains: "JPEG or WebP image file, minimum 2560x1600px"
  key_links:
    - from: "src/app/page.tsx"
      to: "public/hero-bg.jpg"
      via: "Tailwind bg-[url('/hero-bg.jpg')] class on hero section div"
      pattern: "bg-\\[url\\('/hero-bg\\.jpg'\\)\\]"
---

<objective>
Place a hero background image for the landing page hero section and verify it renders correctly across viewports.

Purpose: The hero section code already references `/hero-bg.jpg` with `bg-cover bg-center` and a dark gradient overlay. This plan sources the image asset and verifies the full visual result.
Output: `/public/hero-bg.jpg` placed, hero section visually verified on mobile and desktop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-image/02-UI-SPEC.md
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source and place hero background image</name>
  <files>public/hero-bg.jpg</files>
  <read_first>
    - src/app/page.tsx (lines 83-118 — hero section markup, confirms bg-[url('/hero-bg.jpg')] reference)
    - .planning/phases/02-hero-image/02-UI-SPEC.md (Hero Image Contract section — image requirements)
  </read_first>
  <action>
Source a high-quality hero background image and place it at `/public/hero-bg.jpg`.

Image requirements (from UI-SPEC):
- Format: JPEG (preferred) or WebP
- Minimum resolution: 2560 x 1600px (covers largest retina viewport)
- Subject: Medals — marathon completion medals, award medals, or manufacturing process
- Mood: Professional, premium, dark-toned (overlay will darken further)
- The image must have a clear central subject (medals) that remains visible when cropped via `bg-cover bg-center` at mobile aspect ratios (9:16-ish crop from center)

Sourcing approach (in priority order):
1. Use `curl` to download a royalty-free medal image from Unsplash or Pexels (search for "marathon medal", "gold medal", "award medals")
   - Unsplash direct download URL format: `https://unsplash.com/photos/{id}/download?w=2560`
   - Pexels API if available
2. If no suitable free image is found, create a placeholder using ImageMagick: `convert -size 2560x1600 -seed 42 plasma:gray20-gray40 -blur 0x5 -font Helvetica -pointsize 80 -fill gray60 -gravity center -annotate 0 "HERO IMAGE\n2560x1600\nReplace with medal photo" /Users/william/medal-site/public/hero-bg.jpg`

After placing the file, verify:
- File exists at `/public/hero-bg.jpg`
- File size is reasonable (> 100KB for a real photo, or any size for placeholder)
- Image dimensions are at least 2560x1600 if `identify` (ImageMagick) is available

Note: The code at `src/app/page.tsx` line 85 already has `bg-[url('/hero-bg.jpg')] bg-cover bg-center`. No code changes are needed for the image to display — it loads automatically once the file is in `/public/`.
  </action>
  <verify>
    <automated>test -f /Users/william/medal-site/public/hero-bg.jpg && echo "hero-bg.jpg EXISTS" && ls -lh /Users/william/medal-site/public/hero-bg.jpg</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `public/hero-bg.jpg`
    - File size > 50KB (not an empty or corrupt file)
    - `src/app/page.tsx` line 85 contains `bg-[url('/hero-bg.jpg')]` (no code change needed, just confirm reference is intact)
  </acceptance_criteria>
  <done>Hero background image file placed at /public/hero-bg.jpg, file is a valid image with appropriate dimensions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify hero image renders correctly across viewports</name>
  <files>public/hero-bg.jpg, src/app/page.tsx</files>
  <read_first>
    - public/hero-bg.jpg (the placed image file)
    - src/app/page.tsx (lines 83-118 — hero section to verify)
    - .planning/phases/02-hero-image/02-UI-SPEC.md (Responsive Behavior table and Overlay Requirement)
  </read_first>
  <action>
Start the Next.js dev server with `npm run dev`. Present the user with the verification steps below for desktop and mobile viewports. Wait for user approval or feedback. If user provides a replacement image, place it at `public/hero-bg.jpg` and re-verify.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 | grep -q "200" && echo "Dev server responding"</automated>
  </verify>
  <what-built>
Hero background image placed at /public/hero-bg.jpg. The existing hero section code at src/app/page.tsx already references this file via `bg-[url('/hero-bg.jpg')] bg-cover bg-center` with a dark gradient overlay (`from-black/60 via-black/50 to-black/70`).
  </what-built>
  <how-to-verify>
1. Start the dev server: `npm run dev`
2. Open http://localhost:3000 in a browser

**Desktop check (HERO-01):**
- Hero section shows a medal-related background image filling the full viewport
- The dark gradient overlay renders on top of the image
- White text ("끝까지 해낸 모든 순간을, 메달에 새기다") is clearly readable
- The rose-colored accent text and CTA button are visible
- The "Scroll" hint at the bottom is visible

**Mobile check (HERO-02):**
- Open browser DevTools, toggle device toolbar (Ctrl+Shift+M / Cmd+Shift+M)
- Set to iPhone 14 (390x844) or similar mobile viewport
- Hero background image is visible and centered (medal subject visible in center crop)
- Text remains readable — no text cut off or overlapping
- CTA button "무료 견적 받기" is fully visible and tappable

**Contrast check:**
- White text over the dark overlay maintains readable contrast on the actual image
- No bright image areas "bleeding through" the overlay making text hard to read

**If the image is a placeholder (gray gradient with text):**
- Confirm placeholder renders correctly, then provide a real medal photo to replace it
- Place your photo at `public/hero-bg.jpg` (minimum 2560x1600px, JPEG)
  </how-to-verify>
  <resume-signal>Type "approved" if the hero image looks good on both desktop and mobile. If issues exist, describe them (e.g., "image too bright", "text hard to read", "wrong subject matter") or provide a replacement image.</resume-signal>
  <acceptance_criteria>
    - User confirms hero image displays correctly on desktop viewport
    - User confirms hero image crops/resizes acceptably on mobile viewport
    - User confirms white text is readable over the image with overlay
  </acceptance_criteria>
  <done>User has approved the hero background image rendering on both desktop and mobile viewports</done>
</task>

</tasks>

<verification>
Phase-level verification after all tasks complete:
1. `test -f public/hero-bg.jpg` — image asset exists
2. `grep -n "bg-\[url('/hero-bg.jpg')\]" src/app/page.tsx` — code reference intact (should be line 85)
3. `npm run build` — Next.js build succeeds with the image in place
4. User has visually confirmed the hero renders correctly (Task 2 checkpoint)
</verification>

<success_criteria>
- HERO-01: Hero section displays a medal-related background image on desktop (verified by user in checkpoint)
- HERO-02: Background image displays responsively on mobile/desktop (verified by user in checkpoint)
- Hero text remains readable with dark overlay over the actual image
- No code changes to page.tsx were needed (image asset placement only)
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-image/02-01-SUMMARY.md`
</output>
