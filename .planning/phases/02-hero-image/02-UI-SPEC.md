---
phase: 2
slug: hero-image
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-26
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for Phase 2: Hero Image.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — raw Tailwind v4 with @theme inline tokens |
| Icon library | lucide-react 1.6.0 |
| Font | Pretendard, -apple-system, BlinkMacSystemFont, system-ui, sans-serif |

> Source: `globals.css` @theme inline block + `postcss.config.mjs`. No `components.json` found. shadcn not applicable for an image-placement phase on a fully custom Tailwind codebase.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions:
- Hero content top padding: `pt-16` (64px) — accounts for fixed header height of 64px (`h-16` in Header.tsx)
- Scroll hint bottom offset: `bottom-8` (32px) — fixed absolute position within hero

> Source: pre-populated from existing `page.tsx` hero section layout. No new spacing introduced.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Hero eyebrow label | 12px (xs) / 14px (sm) responsive | 600 semibold | 1.5 (leading-relaxed) |
| Hero heading (h1) | 30px (3xl) / 48px (5xl) / 60px (6xl) responsive | 700 bold | 1.2 (leading-[1.2]) |
| Hero subhead (body) | 14px (sm) / 16px (base) responsive | 400 regular | 1.625 (leading-relaxed) |
| CTA button label | 14px (sm) | 600 semibold | n/a |

> Source: pre-populated from `page.tsx` lines 89–109. No typography changes in this phase — hero text is already implemented. This phase adds only the background image.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#fafaf8` (warm-white) | Page background, surfaces |
| Secondary (30%) | `#f3f2ef` (warm-gray) | Cards, alternate section backgrounds |
| Accent (10%) | `#c4956a` (rose) | Hero eyebrow text, hero h1 accent span, CTA button, scroll-hint gradient |
| Destructive | n/a | No destructive actions in this phase |

Accent reserved for:
- Hero eyebrow label text (`text-rose`)
- `<span>메달에 새기다</span>` within h1 (`text-rose`)
- "무료 견적 받기" primary CTA button background (`bg-rose`)
- Scroll-hint vertical line gradient origin (`from-white/30`)

Hero overlay (not a theme color — applied via inline CSS class):
- `.hero-overlay` in `globals.css`: `linear-gradient(to bottom, rgba(0,0,0,0.55) 0%, rgba(0,0,0,0.45) 50%, rgba(0,0,0,0.65) 100%)`
- Alternative inline Tailwind already in page.tsx: `bg-gradient-to-b from-black/60 via-black/50 to-black/70`
- **Contract**: Use the inline Tailwind gradient (already in code). The overlay must maintain minimum 4.5:1 contrast ratio for white hero text against the image beneath it.

> Source: `globals.css` @theme inline. Overlay values from `page.tsx` line 86.

---

## Hero Image Contract

This section is specific to Phase 2 and describes the image placement contract.

### Image File

| Property | Value |
|----------|-------|
| Filename | `hero-bg.jpg` |
| Location | `/public/hero-bg.jpg` |
| Format | JPEG (preferred) or WebP |
| Minimum resolution | 2560 × 1600px (covers largest retina viewport) |
| Subject | Medals — marathon completion medals, award medals, or manufacturing process |
| Mood | Professional, premium, dark-toned (overlay will darken further) |

### Responsive Behavior

| Viewport | CSS Behavior | Expected result |
|----------|-------------|-----------------|
| Mobile (< 640px) | `bg-cover bg-center` | Image crops to center; subject remains visible |
| Tablet (640–1024px) | `bg-cover bg-center` | Image scales up; no letterboxing |
| Desktop (> 1024px) | `bg-cover bg-center` | Full-bleed background; minimum height `min-h-screen` |

> The existing code at `page.tsx` line 85 already declares `bg-[url('/hero-bg.jpg')] bg-cover bg-center`. No code change is required for responsive behavior — only the image file must be placed in `/public/`.

### Overlay Requirement

The dark gradient overlay (`from-black/60 via-black/50 to-black/70`) is already in place at `page.tsx` line 86. It must remain unchanged. White hero text (`text-white`) over this overlay meets WCAG AA contrast.

### Fallback State

If the image fails to load, the dark gradient still renders via the stacked `div` at line 86. The hero is readable without the background image.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | 무료 견적 받기 (Free Quote) |
| Hero eyebrow | Medal of Finisher |
| Hero heading line 1 | 끝까지 해낸 모든 순간을, |
| Hero heading line 2 (accent) | 메달에 새기다 |
| Hero subhead | 마라톤, 체육대회, 시상식 — 당신의 성취를 정교한 메달로 기록합니다 |
| Scroll hint label | Scroll |
| Empty state heading | n/a — hero always renders |
| Empty state body | n/a — hero always renders |
| Error state | n/a — image load failure degrades gracefully; no error UI needed |
| Destructive confirmation | n/a — no destructive actions in this phase |

> Source: pre-populated from `page.tsx` lines 89–116. No copy changes in this phase.

---

## Interaction Contract

| Interaction | Behavior |
|-------------|----------|
| Page load | Hero renders immediately; `.anim-fade-up` animations fire in sequence (d1→d2→d3 delays: 0.1s, 0.25s, 0.4s) |
| Image load | Background image loads asynchronously; gradient overlay visible immediately during load |
| CTA hover | `hover:bg-rose-dark` color shift + `hover:shadow-lg hover:shadow-rose/25` |
| Scroll past hero | Header transitions from transparent to `bg-white/95 backdrop-blur-md shadow-sm` at `scrollY > 40` |
| Scroll hint | Static; no click interaction; purely decorative |

> Source: pre-populated from existing Header.tsx scroll listener and page.tsx animation classes.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

---

## Implementation Checklist

These are the only deliverables for Phase 2:

1. Source or create `hero-bg.jpg` (minimum 2560×1600px, medal subject, professional quality)
2. Place file at `/public/hero-bg.jpg`
3. Verify `bg-[url('/hero-bg.jpg')] bg-cover bg-center` renders correctly on mobile and desktop
4. Confirm white text contrast is maintained over the overlay on the actual image
5. No code changes required if the image is correctly sized and centered

> The hero section code is complete. This phase is purely an asset placement task.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
