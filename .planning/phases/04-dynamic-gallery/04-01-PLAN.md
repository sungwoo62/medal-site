---
phase: 04-dynamic-gallery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/gallery.ts
  - src/app/api/gallery/route.ts
  - src/app/api/secure/files/route.ts
  - supabase/gallery-seed.sql
autonomous: true
requirements: [GALLERY-01, GALLERY-02, GALLERY-04]

must_haves:
  truths:
    - "gallery_items 테이블이 Supabase에 존재하고 12개 seed 데이터가 들어 있다"
    - "GET /api/gallery 가 gallery_items 목록을 JSON으로 반환한다"
    - "GET /api/secure/files?bucket=gallery&path=... 가 gallery 버킷 signed URL을 반환한다"
  artifacts:
    - path: "src/lib/supabase/gallery.ts"
      provides: "GalleryItem type + fetchGalleryItems function"
      exports: ["GalleryItem", "fetchGalleryItems"]
    - path: "src/app/api/gallery/route.ts"
      provides: "GET endpoint for gallery items"
      exports: ["GET"]
    - path: "supabase/gallery-seed.sql"
      provides: "SQL seed script for 12 gallery items"
      contains: "INSERT INTO gallery_items"
  key_links:
    - from: "src/lib/supabase/gallery.ts"
      to: "supabase gallery_items table"
      via: "supabase.from('gallery_items').select()"
      pattern: "from\\('gallery_items'\\)"
    - from: "src/app/api/gallery/route.ts"
      to: "src/lib/supabase/gallery.ts"
      via: "import fetchGalleryItems"
      pattern: "import.*fetchGalleryItems"

user_setup:
  - service: supabase
    why: "gallery_items 테이블 및 gallery 버킷 생성"
    dashboard_config:
      - task: "Run gallery-seed.sql in Supabase SQL Editor to create table + insert seed data"
        location: "Supabase Dashboard -> SQL Editor"
      - task: "Create 'gallery' storage bucket (private)"
        location: "Supabase Dashboard -> Storage -> New bucket"
      - task: "Add RLS policy for gallery_items: allow SELECT for anon role"
        location: "Supabase Dashboard -> Authentication -> Policies"
---

<objective>
gallery_items 데이터 모델, API 라우트, seed 데이터를 생성하여 동적 갤러리의 백엔드 인프라를 구축한다.

Purpose: 갤러리 페이지가 하드코딩 데이터 대신 Supabase에서 데이터를 가져올 수 있는 기반을 마련한다.
Output: GalleryItem 타입, fetchGalleryItems 함수, GET /api/gallery 엔드포인트, signed URL 프록시 확장, SQL seed 스크립트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dynamic-gallery/04-CONTEXT.md
@.planning/phases/04-dynamic-gallery/04-UI-SPEC.md

@src/lib/supabase/server.ts
@src/app/api/secure/files/route.ts
@src/app/gallery/page.tsx
</context>

<interfaces>
<!-- Existing contracts the executor needs -->

From src/lib/supabase/server.ts:
```typescript
export function createServerClient(): SupabaseClient
```

From src/app/api/secure/files/route.ts:
```typescript
export async function GET(req: NextRequest): Promise<NextResponse>
// Currently only supports 'attachments' bucket, needs extension for 'gallery'
```

From src/app/gallery/page.tsx (ITEMS array to migrate):
```typescript
const ITEMS = [
  { title: string, cat: string, year: string, desc: string }
  // 12 items total
]
const CATEGORIES = ['전체', '마라톤', '체육대회', '시상식', '기업행사']
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: GalleryItem 타입 및 데이터 접근 레이어 생성</name>
  <files>src/lib/supabase/gallery.ts</files>
  <read_first>
    - src/lib/supabase/server.ts
    - src/app/gallery/page.tsx
  </read_first>
  <action>
Create `src/lib/supabase/gallery.ts` with the following:

1. **GalleryItem 타입 정의** (per D-01):
```typescript
export type GalleryItem = {
  id: string
  title: string
  category: string
  year: string
  description: string
  image_url: string | null
  display_order: number
  is_featured: boolean
  created_at: string
}
```

2. **fetchGalleryItems 함수**:
```typescript
export async function fetchGalleryItems(): Promise<GalleryItem[]> {
  const supabase = createServerClient()
  const { data, error } = await supabase
    .from('gallery_items')
    .select('id, title, category, year, description, image_url, display_order, is_featured, created_at')
    .order('display_order', { ascending: true })

  if (error) {
    throw new Error(`갤러리 데이터를 불러올 수 없습니다: ${error.message}`)
  }

  return data ?? []
}
```

3. **fetchFeaturedGalleryItems 함수** (for landing page excerpt):
```typescript
export async function fetchFeaturedGalleryItems(): Promise<GalleryItem[]> {
  const supabase = createServerClient()
  const { data, error } = await supabase
    .from('gallery_items')
    .select('id, title, category, year, description, image_url, display_order, is_featured, created_at')
    .eq('is_featured', true)
    .order('display_order', { ascending: true })

  if (error) {
    throw new Error(`갤러리 데이터를 불러올 수 없습니다: ${error.message}`)
  }

  return data ?? []
}
```

Import `createServerClient` from `@/lib/supabase/server`.
  </action>
  <verify>
    <automated>grep -n "export type GalleryItem" src/lib/supabase/gallery.ts && grep -n "export async function fetchGalleryItems" src/lib/supabase/gallery.ts && grep -n "export async function fetchFeaturedGalleryItems" src/lib/supabase/gallery.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/supabase/gallery.ts contains `export type GalleryItem`
    - src/lib/supabase/gallery.ts contains `export async function fetchGalleryItems`
    - src/lib/supabase/gallery.ts contains `export async function fetchFeaturedGalleryItems`
    - GalleryItem type has fields: id, title, category, year, description, image_url, display_order, is_featured, created_at
    - fetchGalleryItems orders by display_order ascending
    - fetchFeaturedGalleryItems filters by is_featured = true
    - Both functions import createServerClient from @/lib/supabase/server
  </acceptance_criteria>
  <done>GalleryItem 타입 exported, fetchGalleryItems/fetchFeaturedGalleryItems 함수가 gallery_items 테이블에서 데이터를 조회한다</done>
</task>

<task type="auto">
  <name>Task 2: Gallery API 라우트 + signed URL 프록시 확장 + SQL seed 스크립트</name>
  <files>src/app/api/gallery/route.ts, src/app/api/secure/files/route.ts, supabase/gallery-seed.sql</files>
  <read_first>
    - src/app/api/secure/files/route.ts
    - src/app/gallery/page.tsx
    - src/lib/supabase/gallery.ts
  </read_first>
  <action>
**A. Create `src/app/api/gallery/route.ts`** — GET endpoint:
```typescript
import { NextResponse } from 'next/server'
import { fetchGalleryItems } from '@/lib/supabase/gallery'

export async function GET() {
  try {
    const items = await fetchGalleryItems()
    return NextResponse.json(items)
  } catch (error) {
    return NextResponse.json(
      { error: '갤러리를 불러올 수 없습니다.' },
      { status: 500 }
    )
  }
}
```

**B. Extend `src/app/api/secure/files/route.ts`** (per D-03):
Add a `bucket` query parameter. If `bucket` is provided and is one of the allowed values (`attachments`, `gallery`), use that bucket. Otherwise default to `attachments` for backward compatibility.

Change:
```typescript
const { data, error } = await supabase.storage
  .from('attachments')
  .createSignedUrl(path, 60 * 60)
```
To:
```typescript
const bucket = req.nextUrl.searchParams.get('bucket') ?? 'attachments'
const ALLOWED_BUCKETS = ['attachments', 'gallery']
if (!ALLOWED_BUCKETS.includes(bucket)) {
  return NextResponse.json({ error: '허용되지 않은 버킷입니다.' }, { status: 400 })
}
const { data, error } = await supabase.storage
  .from(bucket)
  .createSignedUrl(path, 60 * 60)
```

**C. Create `supabase/gallery-seed.sql`** (per D-06):
SQL script with:
1. CREATE TABLE IF NOT EXISTS gallery_items:
   - id: uuid DEFAULT gen_random_uuid() PRIMARY KEY
   - title: text NOT NULL
   - category: text NOT NULL
   - year: text NOT NULL
   - description: text NOT NULL
   - image_url: text (nullable — images uploaded later via allpack-ops)
   - display_order: integer NOT NULL DEFAULT 0
   - is_featured: boolean NOT NULL DEFAULT false
   - created_at: timestamptz NOT NULL DEFAULT now()

2. RLS enable + policy:
   - ALTER TABLE gallery_items ENABLE ROW LEVEL SECURITY;
   - CREATE POLICY "gallery_items_select_anon" ON gallery_items FOR SELECT TO anon USING (true);
   - CREATE POLICY "gallery_items_all_service" ON gallery_items FOR ALL TO service_role USING (true);

3. INSERT 12 rows from existing ITEMS array in gallery/page.tsx:
```sql
INSERT INTO gallery_items (title, category, year, description, display_order, is_featured) VALUES
('서울국제마라톤 완주메달', '마라톤', '2024', '금도금 다이캐스팅, 리본 포함 1,200개', 1, true),
('전국체육대회 금메달', '체육대회', '2024', '순금 도금, 고급 케이스 포함 300개', 2, true),
('기업 우수사원 시상패', '시상식', '2024', '아크릴+금속 복합소재, 레이저 각인 50개', 3, true),
('창립 30주년 기념메달', '기업행사', '2024', '순은 도금, 한정판 시리얼 넘버 500개', 4, true),
('부산 해변마라톤 메달', '마라톤', '2024', '앤틱실버 도금, 커스텀 리본 2,000개', 5, true),
('전국 수영대회 메달세트', '체육대회', '2023', '금/은/동 3종, 각 100개', 6, true),
('올해의 교사상 시상메달', '시상식', '2023', '고급 금도금, 벨벳 케이스 30개', 7, false),
('스타트업 해커톤 메달', '기업행사', '2023', '무광 블랙+골드 2톤, 200개', 8, false),
('춘천마라톤 기념메달', '마라톤', '2023', '풀컬러 에폭시, 3,000개 대량 제작', 9, false),
('도민체전 종합우승 메달', '체육대회', '2023', '프리미엄 금도금, 150개', 10, false),
('봉사 공로상 메달', '시상식', '2023', '앤틱골드, 나무 케이스 포함 80개', 11, false),
('연말 시상식 메달세트', '기업행사', '2023', '다이캐스팅+에나멜, 금/은/동 각 50개', 12, false);
```

Mark first 6 items as is_featured=true (matching GALLERY_ITEMS on landing page).
  </action>
  <verify>
    <automated>grep -n "export async function GET" src/app/api/gallery/route.ts && grep -n "ALLOWED_BUCKETS" src/app/api/secure/files/route.ts && grep -c "INSERT INTO gallery_items" supabase/gallery-seed.sql && grep -c "CREATE TABLE" supabase/gallery-seed.sql</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/gallery/route.ts contains `export async function GET`
    - src/app/api/gallery/route.ts imports fetchGalleryItems from @/lib/supabase/gallery
    - src/app/api/secure/files/route.ts contains `ALLOWED_BUCKETS` with 'attachments' and 'gallery'
    - src/app/api/secure/files/route.ts still works without bucket param (defaults to 'attachments')
    - supabase/gallery-seed.sql contains CREATE TABLE IF NOT EXISTS gallery_items
    - supabase/gallery-seed.sql contains ENABLE ROW LEVEL SECURITY
    - supabase/gallery-seed.sql contains 12 INSERT values matching the ITEMS array titles
    - supabase/gallery-seed.sql contains is_featured boolean column
    - supabase/gallery-seed.sql contains display_order integer column
    - TypeScript compilation: `npx tsc --noEmit --pretty` exits 0
  </acceptance_criteria>
  <done>GET /api/gallery 엔드포인트가 존재하고, signed URL 프록시가 gallery 버킷을 지원하며, SQL seed 스크립트에 12개 항목이 포함된다</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --pretty` exits 0 (no type errors)
- `grep -r "GalleryItem" src/` shows type usage across files
- `supabase/gallery-seed.sql` contains 12 INSERT rows
- `src/app/api/secure/files/route.ts` supports both `attachments` and `gallery` buckets
</verification>

<success_criteria>
- GalleryItem 타입이 정의되고 export 된다
- fetchGalleryItems, fetchFeaturedGalleryItems 함수가 gallery_items 테이블을 조회한다
- GET /api/gallery 가 gallery_items 전체 목록을 JSON으로 반환한다
- signed URL 프록시가 gallery 버킷도 지원한다 (per D-03)
- SQL seed 스크립트에 테이블 생성 + RLS + 12개 데이터 INSERT가 포함된다 (per D-06)
</success_criteria>

<output>
After completion, create `.planning/phases/04-dynamic-gallery/04-01-SUMMARY.md`
</output>
