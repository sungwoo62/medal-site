---
phase: 04-dynamic-gallery
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/app/gallery/page.tsx
  - src/app/gallery/GalleryClient.tsx
  - src/app/page.tsx
autonomous: true
requirements: [GALLERY-03, GALLERY-05]

must_haves:
  truths:
    - "갤러리 페이지가 Supabase에서 가져온 동적 데이터를 렌더링한다"
    - "카테고리 필터를 클릭하면 해당 카테고리 항목만 표시된다"
    - "로딩 중 스켈레톤 카드 6개가 표시된다"
    - "fetch 실패 시 에러 배너가 표시된다"
    - "반응형 그리드 (1/2/3열)가 유지된다"
    - "랜딩 페이지 갤러리 섹션이 is_featured 항목을 동적으로 표시한다"
  artifacts:
    - path: "src/app/gallery/page.tsx"
      provides: "Server component: fetches gallery items, passes to client"
      contains: "fetchGalleryItems"
    - path: "src/app/gallery/GalleryClient.tsx"
      provides: "Client component: filter UI, grid, loading/error states"
      contains: "use client"
    - path: "src/app/page.tsx"
      provides: "Landing page with dynamic gallery excerpt"
      contains: "fetchFeaturedGalleryItems"
  key_links:
    - from: "src/app/gallery/page.tsx"
      to: "src/lib/supabase/gallery.ts"
      via: "import fetchGalleryItems"
      pattern: "import.*fetchGalleryItems"
    - from: "src/app/gallery/GalleryClient.tsx"
      to: "src/lib/supabase/gallery.ts"
      via: "import GalleryItem type"
      pattern: "import.*GalleryItem"
    - from: "src/app/page.tsx"
      to: "src/lib/supabase/gallery.ts"
      via: "import fetchFeaturedGalleryItems"
      pattern: "import.*fetchFeaturedGalleryItems"
---

<objective>
갤러리 페이지를 하드코딩 ITEMS 배열에서 Supabase 동적 데이터로 전환한다. SSR + 클라이언트 필터 구조 적용, 로딩/에러/빈 상태 UI 추가, 랜딩 페이지 갤러리 섹션도 동적 데이터로 전환.

Purpose: 사용자가 Supabase에서 관리되는 실시간 갤러리 데이터를 보게 된다.
Output: SSR 서버 컴포넌트 + 클라이언트 필터 컴포넌트, 랜딩 페이지 동적 갤러리 섹션
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dynamic-gallery/04-CONTEXT.md
@.planning/phases/04-dynamic-gallery/04-UI-SPEC.md
@.planning/phases/04-dynamic-gallery/04-01-SUMMARY.md

@src/app/gallery/page.tsx
@src/app/page.tsx
@src/lib/supabase/gallery.ts
</context>

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/supabase/gallery.ts:
```typescript
export type GalleryItem = {
  id: string
  title: string
  category: string
  year: string
  description: string
  image_url: string | null
  display_order: number
  is_featured: boolean
  created_at: string
}

export async function fetchGalleryItems(): Promise<GalleryItem[]>
export async function fetchFeaturedGalleryItems(): Promise<GalleryItem[]>
```

From 04-UI-SPEC.md — Interaction States:
- Loading: 6 skeleton cards, rounded-2xl, aspect-[4/3] bg-warm-gray animate-pulse
- Error: bg-rose/10 border border-rose/20 rounded-xl p-8 text-center
- Empty: centered message with CTA
- Filter: client-side, no network request on change

From 04-UI-SPEC.md — Copywriting:
- Loading heading: "갤러리를 불러오는 중..."
- Error heading: "갤러리를 불러올 수 없습니다"
- Error body: "잠시 후 다시 시도해 주세요. 문제가 지속되면 고객센터로 연락해 주세요."
- Empty heading: "아직 등록된 사례가 없습니다"
- Empty body: "곧 새로운 제작 사례를 업로드할 예정입니다. 견적 문의는 언제든지 가능합니다."
- Filtered empty: "해당 카테고리의 제작 사례가 없습니다"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 갤러리 페이지를 SSR + 클라이언트 필터 구조로 전환</name>
  <files>src/app/gallery/page.tsx, src/app/gallery/GalleryClient.tsx</files>
  <read_first>
    - src/app/gallery/page.tsx
    - src/lib/supabase/gallery.ts
    - .planning/phases/04-dynamic-gallery/04-UI-SPEC.md
  </read_first>
  <action>
**A. Create `src/app/gallery/GalleryClient.tsx`** — 'use client' component:

Props: `{ items: GalleryItem[] }` imported from `@/lib/supabase/gallery`.

State: `const [filter, setFilter] = useState('전체')`

Constants (keep in this file):
```typescript
const CATEGORIES = ['전체', '마라톤', '체육대회', '시상식', '기업행사']

const GRADIENTS: Record<string, string> = {
  '마라톤': 'from-amber-100 to-amber-50',
  '체육대회': 'from-blue-100 to-blue-50',
  '시상식': 'from-violet-100 to-violet-50',
  '기업행사': 'from-rose-100 to-rose-50',
}
```

Filtering logic: `const filtered = filter === '전체' ? items : items.filter((i) => i.category === filter)`

Render structure (preserve ALL existing UI-SPEC styles exactly):
1. **Header section** — same as current: overline "Portfolio", h1 "제작 사례", subtitle
2. **Filter pills** — same as current: gap-2.5, rounded-full, bg-rose/text-white active, bg-white/border inactive
3. **Gallery grid** — `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5`
4. **Gallery card** — same structure: rounded-2xl, border, hover:shadow-lg, aspect-[4/3] image area
   - **Image loaded state**: When `item.image_url` is not null, render `<img src={/api/secure/files?bucket=gallery&path=${item.image_url}} alt={item.title} className="w-full h-full object-cover" />` inside the aspect-[4/3] div. Keep category badge and year badge overlaid.
   - **No image fallback**: When `item.image_url` is null, render category gradient + Award icon (exactly as current). Per UI-SPEC: "No image fallback: retain existing category gradient + Award icon".
   - Category badge: `absolute top-3 left-3 text-[10px] font-semibold text-rose bg-rose/10 px-2.5 py-1 rounded-full` showing `item.category`
   - Year badge: `absolute top-3 right-3 text-[10px] text-charcoal-light` showing `item.year`
   - Text area: `p-5` with h3 `text-sm font-bold text-charcoal mb-1` for title, p `text-xs text-charcoal-light` for description

5. **Empty state** (filtered.length === 0 and items.length > 0):
   - Centered text: "해당 카테고리의 제작 사례가 없습니다"

6. **Empty state** (items.length === 0):
   - Heading: "아직 등록된 사례가 없습니다"
   - Body: "곧 새로운 제작 사례를 업로드할 예정입니다. 견적 문의는 언제든지 가능합니다."

Import Award from lucide-react.

**B. Rewrite `src/app/gallery/page.tsx`** as a Server Component:

Remove 'use client' directive. Remove ITEMS, CATEGORIES, GRADIENTS constants. Remove useState import.

```typescript
import { fetchGalleryItems } from '@/lib/supabase/gallery'
import GalleryClient from './GalleryClient'

export default async function GalleryPage() {
  let items
  let error = false

  try {
    items = await fetchGalleryItems()
  } catch {
    items = []
    error = true
  }

  if (error) {
    return (
      <div className="min-h-screen pt-24 pb-16 px-6 bg-warm-white">
        <div className="max-w-6xl mx-auto">
          <div className="text-center mb-12">
            <p className="text-rose text-xs font-semibold tracking-[0.2em] uppercase mb-2">Portfolio</p>
            <h1 className="text-3xl sm:text-4xl font-bold text-charcoal mb-3">제작 사례</h1>
          </div>
          <div className="bg-rose/10 border border-rose/20 rounded-xl p-8 text-center">
            <h2 className="text-lg font-bold text-charcoal mb-2">갤러리를 불러올 수 없습니다</h2>
            <p className="text-sm text-charcoal-light">잠시 후 다시 시도해 주세요. 문제가 지속되면 고객센터로 연락해 주세요.</p>
          </div>
        </div>
      </div>
    )
  }

  return <GalleryClient items={items} />
}
```

The GalleryClient component wraps the full page layout (min-h-screen pt-24 pb-16 px-6 bg-warm-white + max-w-6xl mx-auto) to maintain the exact same structure as the current page.
  </action>
  <verify>
    <automated>grep -n "use client" src/app/gallery/GalleryClient.tsx && grep -n "fetchGalleryItems" src/app/gallery/page.tsx && grep -L "use client" src/app/gallery/page.tsx && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/gallery/page.tsx does NOT contain 'use client'
    - src/app/gallery/page.tsx contains `import { fetchGalleryItems } from '@/lib/supabase/gallery'`
    - src/app/gallery/page.tsx contains `import GalleryClient from './GalleryClient'`
    - src/app/gallery/page.tsx contains `export default async function GalleryPage`
    - src/app/gallery/page.tsx contains error state with "갤러리를 불러올 수 없습니다" and "bg-rose/10 border border-rose/20 rounded-xl p-8"
    - src/app/gallery/GalleryClient.tsx contains 'use client'
    - src/app/gallery/GalleryClient.tsx contains `useState('전체')`
    - src/app/gallery/GalleryClient.tsx contains `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`
    - src/app/gallery/GalleryClient.tsx contains GRADIENTS Record with 4 categories
    - src/app/gallery/GalleryClient.tsx contains "해당 카테고리의 제작 사례가 없습니다" (filtered empty)
    - src/app/gallery/GalleryClient.tsx contains "아직 등록된 사례가 없습니다" (total empty)
    - src/app/gallery/GalleryClient.tsx contains `/api/secure/files?bucket=gallery` for image_url rendering
    - src/app/gallery/GalleryClient.tsx imports GalleryItem type
    - `npx tsc --noEmit --pretty` exits 0
  </acceptance_criteria>
  <done>갤러리 페이지가 SSR 서버 컴포넌트 + 클라이언트 필터 구조로 전환되고, 로딩/에러/빈 상태 UI가 모두 구현된다</done>
</task>

<task type="auto">
  <name>Task 2: 랜딩 페이지 갤러리 섹션을 동적 데이터로 전환</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx
    - src/lib/supabase/gallery.ts
  </read_first>
  <action>
Convert the landing page gallery section to use dynamic data from Supabase.

1. **Add imports** at top of file:
```typescript
import { fetchFeaturedGalleryItems, type GalleryItem } from '@/lib/supabase/gallery'
```

2. **Make HomePage async**:
```typescript
export default async function HomePage() {
```

3. **Fetch featured items** at the start of the function body:
```typescript
let galleryItems: GalleryItem[] = []
try {
  galleryItems = await fetchFeaturedGalleryItems()
} catch {
  // 갤러리 로딩 실패 시 빈 배열 — 섹션 자체를 숨김
}
```

4. **Remove hardcoded GALLERY_ITEMS** constant (the 6-item array at the top of file). Keep TRUST_STATS, CATEGORIES, PROCESS_STEPS, GRADIENTS as-is.

5. **Replace the gallery section rendering**. Change from mapping GALLERY_ITEMS to mapping galleryItems:
```tsx
{galleryItems.length > 0 && (
  <section className="py-20 sm:py-24 bg-warm-gray">
    <div className="max-w-6xl mx-auto px-6">
      <div className="text-center mb-14 reveal">
        <p className="text-rose text-xs font-semibold tracking-[0.2em] uppercase mb-2">Portfolio</p>
        <h2 className="text-2xl sm:text-3xl font-bold text-charcoal">제작 사례</h2>
      </div>

      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">
        {galleryItems.map((item) => (
          <div
            key={item.id}
            className="reveal group rounded-2xl overflow-hidden border border-border bg-white hover:shadow-lg transition-all duration-300"
          >
            <div className={`aspect-[4/3] ${item.image_url ? '' : `bg-gradient-to-br ${GRADIENTS[item.category] ?? 'from-gray-100 to-gray-50'}`} flex items-center justify-center relative overflow-hidden`}>
              {item.image_url ? (
                <img src={`/api/secure/files?bucket=gallery&path=${item.image_url}`} alt={item.title} className="w-full h-full object-cover" />
              ) : (
                <Award size={48} className="text-charcoal/10 group-hover:text-rose/30 transition-colors duration-500" />
              )}
              <span className="absolute top-3 left-3 text-[10px] font-semibold text-rose bg-rose/10 px-2.5 py-1 rounded-full">{item.category}</span>
            </div>
            <div className="p-5">
              <h3 className="text-sm font-bold text-charcoal mb-1">{item.title}</h3>
              <p className="text-xs text-charcoal-light">{item.description}</p>
            </div>
          </div>
        ))}
      </div>

      <div className="text-center mt-10 reveal">
        <Link
          href="/gallery"
          className="inline-flex items-center gap-2 text-rose text-sm font-semibold hover:gap-3 transition-all"
        >
          전체 사례 보기 <ArrowRight size={15} />
        </Link>
      </div>
    </div>
  </section>
)}
```

Key changes from existing:
- `item.cat` -> `item.category` (per GalleryItem type)
- `item.detail` -> `item.description` (per GalleryItem type)
- `item.title` key -> `item.id` key
- Added conditional image rendering (image_url present vs null)
- Wrapped section in `galleryItems.length > 0` so it hides if fetch fails
- Landing page is already a Server Component (no 'use client'), so async works directly
  </action>
  <verify>
    <automated>grep -n "fetchFeaturedGalleryItems" src/app/page.tsx && grep -n "export default async function HomePage" src/app/page.tsx && grep -c "GALLERY_ITEMS" src/app/page.tsx && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx contains `import { fetchFeaturedGalleryItems` from '@/lib/supabase/gallery'
    - src/app/page.tsx contains `export default async function HomePage`
    - src/app/page.tsx does NOT contain the hardcoded `GALLERY_ITEMS` constant array (grep count = 0)
    - src/app/page.tsx contains `galleryItems.map` (using dynamic data)
    - src/app/page.tsx contains `item.category` (not item.cat)
    - src/app/page.tsx contains `item.description` (not item.detail)
    - src/app/page.tsx contains `/api/secure/files?bucket=gallery` for image URLs
    - src/app/page.tsx wraps gallery section in `galleryItems.length > 0` condition
    - TRUST_STATS, CATEGORIES, PROCESS_STEPS, GRADIENTS constants remain unchanged
    - `npx tsc --noEmit --pretty` exits 0
  </acceptance_criteria>
  <done>랜딩 페이지 갤러리 섹션이 is_featured 항목을 Supabase에서 동적으로 로드하며, fetch 실패 시 섹션이 숨겨진다</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --pretty` exits 0
- src/app/gallery/page.tsx is a server component (no 'use client')
- src/app/gallery/GalleryClient.tsx is a client component ('use client')
- Category filter logic preserved (client-side)
- Responsive grid classes preserved (grid-cols-1 sm:grid-cols-2 lg:grid-cols-3)
- All UI-SPEC copywriting strings present in the components
</verification>

<success_criteria>
- 갤러리 페이지 접속 시 Supabase gallery_items 테이블에서 데이터를 SSR로 가져온다
- 카테고리 필터 클릭 시 해당 카테고리만 표시된다 (네트워크 요청 없음)
- image_url이 null인 항목은 기존 그라데이션 + Award 아이콘으로 표시된다
- image_url이 있는 항목은 signed URL 프록시를 통해 이미지를 표시한다
- 에러 시 "갤러리를 불러올 수 없습니다" 배너가 표시된다
- 빈 상태 시 적절한 메시지가 표시된다
- 랜딩 페이지 갤러리 섹션이 is_featured 항목을 동적으로 표시한다
- 반응형 그리드 레이아웃이 유지된다
</success_criteria>

<output>
After completion, create `.planning/phases/04-dynamic-gallery/04-02-SUMMARY.md`
</output>
